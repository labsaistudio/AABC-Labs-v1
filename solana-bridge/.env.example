# AABC Solana Bridge Configuration

# Server Configuration
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# CORS Configuration
CORS_ORIGIN=http://localhost:8000

# Solana Configuration
SOLANA_NETWORK=mainnet-beta
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Wallet Configuration (Base58 encoded private key)
# Generate new wallet if not provided
SOLANA_PRIVATE_KEY=

# OpenAI Configuration (for agent operations)
OPENAI_API_KEY=

# Optional: Custom RPC endpoints for better performance
# SOLANA_RPC_URL=https://your-custom-rpc.com

# Optional: Helius API for enhanced functionality
# HELIUS_API_KEY=

# Optional: QuickNode endpoint
# QUICKNODE_ENDPOINT=

# IPFS Configuration (Required for Pump.fun token launches)
# Pinata is used to upload token metadata (image, description) to IPFS
# How to get PINATA_JWT:
# 1. Sign up for free account at https://pinata.cloud
# 2. Go to API Keys section
# 3. Create new Admin key
# 4. Copy the JWT token (starts with "eyJ...")
# Without this, tokens will be created without metadata on Pump.fun
PINATA_JWT=

# CoinGecko Market Data API (Recommended for price data and market insights)
# Enables real-time token prices, trending tokens, top gainers, and pool data
# How to get COINGECKO_DEMO_API_KEY (FREE):
# 1. Sign up at https://www.coingecko.com/en/developers/dashboard
# 2. Create new API Key with "Demo" plan (completely free)
# 3. Copy the API key (format: CG-xxxxxxxxxx)
# Demo API provides: 10-50 calls/minute (sufficient for most use cases)
# For Pro API ($199/month): Higher rate limits and historical data
COINGECKO_DEMO_API_KEY=
COINGECKO_PRO_API_KEY=
BLINKS_BASE_URL=https://aabc-solana-bridge-production.up.railway.app
